<!doctype html>
<html>
<head>
    <title>Table Game Portal</title>
    <meta name = "viewport" content = "width=device-width, initial-scale = 1"> <!-- do we need here for mobile or is it placed somewhere else? -->
    <link rel="stylesheet" type="text/css" href="css/styles.css"><!-- main styles for page -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css"> <!-- load fontawesome -->
        <!-- we need JQUERY to do JQUERY tricks -->
    <script src="jquery-1.11.3.js" type="text/javascript"></script>
    <script>

//LOGIN BUTTON POST FUNCTION
$("login").click(function(){
    $.post("/login",
    {
        name: "Donald Duck",
        city: "Duckburg"
    },
    function(data, status){
        alert("Data: " + data + "\nStatus: " + status);
    });
});

//SINGUP BUTTON POST FUNCTION
$("submit").click(function(){
    $.post("/singup",
    {
        name: "Donald Duck",
        city: "Duckburg"
    },
    function(data, status){
        alert("Data: " + data + "\nStatus: " + status);
    });
});
//GETTINV VALUE FROM TEMPLATING ENGINE
var condition = <%- JSON.stringify(condition) %>;
console.log(condition);

//ON DOCUMENT READY
$( document ).ready(function() {
    if(condition==='login'){
        $('#singup_form').css('display','none')
    }
    else if(condition==='singup'){
        $('#login_form').css('display','none')
    }
    else if(condition==='logged'){
        
    }
});

    </script>
</head>
<body>
<div id='login_form' class="col-sm-6 col-sm-offset-3">
    <h1><span class="fa fa-sign-in"></span> Login</h1>
    <!-- show any messages that come back with authentication -->
    <% if (messagelog.length > 0) { %>
        <div class="alert alert-danger"><%= messagelog %></div>
    <% } %>
    <!-- LOGIN FORM -->
    <form>
        <div class="form-group">
            <label>Email</label>
            <input type="text" class="form-control" name="email">
        </div>
        <div class="form-group">
            <label>Password</label>
            <input type="password" class="form-control" name="password">
        </div>
        <button id='login'class="btn btn-warning btn-lg">Login</button>
    </form>
    <hr>
    <p>Need an account? <a href="/signup">Signup</a></p>

</div>


<div id='singup_form' class="col-sm-6 col-sm-offset-3">
    <h1><span class="fa fa-sign-in"></span> Signup</h1>
    <!-- show any messages that come back with authentication -->
    <% if (messagesign.length > 0) { %>
        <div class="alert alert-danger"><%= messagesingn %></div>
    <% } %>
    <!-- LOGIN FORM -->
    <form>
        <div class="form-group">
            <label>Email</label>
            <input type="text" class="form-control" name="email">
        </div>
        <div class="form-group">
            <label>Password</label>
            <input type="password" class="form-control" name="password">
        </div>
        
        <button id='submit' class="btn btn-warning btn-lg">Signup</button>
    </form>
    <hr>
    <p>Already have an account? <a href="/login">Login</a></p>
    <p>Or go <a href="/">home</a>.</p>
</div>
</body>
</html>